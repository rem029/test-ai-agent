# Static Assets for AgentFlow Web UI

These files are served by the FastAPI app in `src/agentflow/web/app.py`.

- `index.html` - main page
- `styles.css` - styling
- `app.js` - client-side JavaScript
