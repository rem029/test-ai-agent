# Copy to agentflow.config.yaml (gitignored) and adjust per role.
# Each role can independently use: claude-code | antigravity | openrouter
# Env var overrides win over this file: AGENTFLOW_<ROLE>_BACKEND / AGENTFLOW_<ROLE>_MODEL
# Default mapping below is a starting point, not finalized — see PLAN.md, Open items.

review:
  backend: claude-code

build:
  backend: antigravity

verify:
  backend: claude-code

# Example OpenRouter override for the build role (cheap/fast model, requires
# OPENROUTER_API_KEY in your environment — see .env, gitignored):
# build:
#   backend: openrouter
#   model: deepseek/deepseek-v4-flash  # unverified slug, confirm at openrouter.ai/models
